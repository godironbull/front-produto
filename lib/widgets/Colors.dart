// ignore: file_names
// ignore: file_names
import 'package:flutter/material.dart';

class QueryColor {
  static Size querymedia(BuildContext context) {
    return MediaQuery.of(context).size;
  }

  static Color primary = const Color(0xFF006064); // Azul escuro (para títulos)
  static Color secondary =
      Color(0xFF424242); // Cinza escuro (para texto secundário)
  static Color background = Color(0xFFE0F7FA); // Azul claro (fundo)
  static Color textColor =
      Color(0xFF424242); // Cinza escuro (para o texto principal)
  static Color backgroundcolor =Colors.blueGrey;
}
